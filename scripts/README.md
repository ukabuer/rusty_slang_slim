# Maintainer scripts

This directory is reserved for reproducible Slang fetch, native build, packaging, checksum, and release-validation scripts.

Consumer `cargo build` must not invoke these scripts.

